create a counter 
use inner html to to call the quote according to the counter
quotes.length if statement functions to switch between one to the OES_texture_half_float_linear
the buttons are variables 
call the button as EventListeners 
use quote timer at the encodeURI

if counter === 0
    counter = quotes.length - 1
else 
     counter --


imbed the documents in function for the quote